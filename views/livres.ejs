<%- include("_header") -%>

<h1>Listado de pines (<%= model.length %>)</h1>

<div class="table-responsive-sm">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Titulo</th>
        <th>Ojo a</th>
        <th>Comentarios</th>
        <th class="d-print-none">
          <a class="btn btn-sm btn-success" href="/create">Nuevo pin</a>
        </th>
      </tr>
    </thead>
    <tbody>
      <% for (const book of model) { %>
        <tr>
          <td><%= book.Titre %></td>
          <td><%= book.Auteur %></td>
          <td><%= book.Commentaires %></td>
          <td class="d-print-none">
            <a class="btn btn-sm btn-warning" href="/edit/<%= book.Livre_ID %>">Modificar</a>
            <a class="btn btn-sm btn-danger" href="/delete/<%= book.Livre_ID %>">Eliminar</a>
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

<%- include("_footer") -%>
